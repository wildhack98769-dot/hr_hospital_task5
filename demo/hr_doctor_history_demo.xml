<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="demo_history_house" model="hr.hospital.doctor.history">
            <field name="patient_id" ref="patient_thirteen"/>
            <field name="doctor_id" ref="doctor_mentor_gregory_house"/>
            <field name="appointment_date" eval="datetime.now().strftime('%Y-%m-%d')"/>
            <field name="change_date" eval="(datetime.now() + relativedelta(days=1)).strftime('%Y-%m-%d')"/>
            <field name="active">True</field>
        </record>

        <record id="demo_history_house1" model="hr.hospital.doctor.history">
            <field name="patient_id" ref="patient_thirteen"/>
            <field name="doctor_id" ref="doctor_intern_allison_cameron"/>
            <field name="appointment_date" eval="datetime.now().strftime('%Y-%m-%d')"/>
            <field name="change_date" eval="(datetime.now() + relativedelta(days=2)).strftime('%Y-%m-%d')"/>
            <field name="active">True</field>
        </record>

        <record id="demo_history_house2" model="hr.hospital.doctor.history">
            <field name="patient_id" ref="patient_thirteen"/>
            <field name="doctor_id" ref="doctor_cuddy"/>
            <field name="appointment_date" eval="datetime.now().strftime('%Y-%m-%d')"/>
            <field name="change_date" eval="(datetime.now() + relativedelta(days=3)).strftime('%Y-%m-%d')"/>
            <field name="active">True</field>
        </record>

    </data>
</odoo>
